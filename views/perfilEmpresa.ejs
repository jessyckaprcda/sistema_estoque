<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Perfil da Empresa</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      margin: 0;
      font-family: Arial, sans-serif;
      background-color: #b71c1c;
      color: white;
      display: flex;
      flex-direction: column;
      align-items: center;
      min-height: 100vh;
    }
    .container {
      margin-top: 80px;
    }
    .card {
      background-color: white;
      color: #333;
      border-radius: 10px;
      padding: 20px;
      width: 100%;
      max-width: 600px;
      box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.3);
    }
    .btn-back {
      background-color: #8b0000;
      color: white;
    }
    .btn-back:hover {
      background-color: #a50000;
      color: white;
    }
  </style>
</head>
<body>
  <div class="container mt-4 text-center">
    <h1 class="mb-4">Perfil da Empresa</h1>
    <div class="card mx-auto">
      <h3 class="mb-3">Dados da Empresa</h3>
      <p><strong>Nome:</strong> <%= empresa.nome_empresa %></p>
      <p><strong>Email:</strong> <%= empresa.email %></p>
      <p><strong>Telefone:</strong> <%= empresa.telefone %></p>
      <p><strong>Endereço:</strong> <%= empresa.endereco %></p>
      <p><strong>CNPJ:</strong> <%= empresa.cnpj %></p>
    </div>
    <a href="/index/<%= empresa.id_empresa %>" class="btn btn-danger btn-sm me-2">Voltar</a>
    
    <a href="/empresa/editar/<%= empresa.id_empresa %>" class="btn btn-danger btn-sm me-2">Editar</a>
    <form action="/empresa/deletar/<%= empresa.id_empresa %>" method="POST" style="display:inline;">
        <button type="submit" class="btn btn-danger btn-sm">Deletar</button>
    </form>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
