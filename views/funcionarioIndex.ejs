<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Página do Funcionário</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      margin: 0;
      background-color: #b71c1c;
      color: white;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      text-align: center;
    }
    header {
      width: 100%;
      max-width: 600px;
      padding: 20px;
      background-color: white;
      color: #b71c1c;
      border-radius: 10px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
    }
    .btn-custom {
      background-color: #007bff;
      color: white;
    }
    .btn-custom:hover {
      background-color: #0056b3;
    }
    .btn-home {
      background-color: white;
      color: #b71c1c;
      border: 1px solid white;
      transition: background-color 0.3s ease, color 0.3s ease;
    }
    .btn-home:hover {
      background-color: #f5f5f5;
      color: #b71c1c;
    }
  </style>
</head>
<body>
  <header class="container text-center">
    <h1>Bem-vindo, <%= funcionario.nome_funcionario %>!</h1>
    <p><strong>Cargo:</strong> <%= funcionario.cargo || "Não informado" %></p>
    <div class="d-flex flex-column gap-2 mt-4">
      <a class="btn btn-home w-100" href="/movimentacao/<%= funcionario.id_funcionario %>">Registrar Movimentações</a>
      <a class="btn btn-home w-100" href="/movimentacao/funcionario/<%= funcionario.id_funcionario %>/movimentacoes">Ver Movimentações</a>
      <a class="btn btn-home w-100" href="/">Logout</a>
    </div>
  </header>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>